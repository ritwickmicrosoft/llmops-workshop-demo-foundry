# =============================================================================
# Microsoft Foundry LLMOps Workshop - Environment Configuration
# =============================================================================
# This file contains all configuration for using Microsoft Foundry platform
# Copy this file to .env and fill in your values
#
# NO AZURE OPENAI RESOURCE REQUIRED - All inference goes through Foundry!

# -----------------------------------------------------------------------------
# Microsoft Foundry Configuration (Primary)
# -----------------------------------------------------------------------------
# The project endpoint from Foundry portal (Overview > Endpoints and keys)
FOUNDRY_PROJECT_ENDPOINT=https://foundry-llmops-canadaeast.services.ai.azure.com/api/projects/proj-llmops-demo

# Your Foundry project name (for display purposes)
FOUNDRY_PROJECT_NAME=proj-llmops-demo

# -----------------------------------------------------------------------------
# Azure Subscription Details (Optional - only for evaluation upload)
# -----------------------------------------------------------------------------
# These are only needed if you want to upload evaluation results to Foundry portal
AZURE_SUBSCRIPTION_ID=1d53bfb3-a84c-4eb4-8c79-f29dc8424b6a
AZURE_RESOURCE_GROUP=rg-llmops-canadaeast

# -----------------------------------------------------------------------------
# Model Configuration (deployed via Foundry Model Catalog)
# -----------------------------------------------------------------------------
# Chat completion model deployment name
CHAT_MODEL=gpt-4o

# Embedding model deployment name  
EMBEDDING_MODEL=text-embedding-3-large

# -----------------------------------------------------------------------------
# Azure AI Search Configuration
# -----------------------------------------------------------------------------
AZURE_SEARCH_ENDPOINT=https://search-llmops-canadaeast.search.windows.net
AZURE_SEARCH_INDEX_NAME=walle-products

# -----------------------------------------------------------------------------
# Evaluation Settings
# -----------------------------------------------------------------------------
# Maximum samples to evaluate (helps avoid rate limits during demos)
EVAL_MAX_SAMPLES=5

# Set to true to upload evaluation results to Foundry portal
EVAL_UPLOAD_TO_PORTAL=false

# -----------------------------------------------------------------------------
# Authentication Notes
# -----------------------------------------------------------------------------
# This project uses RBAC authentication (no API keys required)
# 
# Locally: Uses Azure CLI credentials (run 'az login' first)
# In Azure: Uses Managed Identity
#
# Required RBAC roles:
# - Cognitive Services OpenAI User (on Foundry resource)
# - Search Index Data Contributor (on AI Search resource)
